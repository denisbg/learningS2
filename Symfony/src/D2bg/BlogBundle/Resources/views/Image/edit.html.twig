{% extends '::layout.html.twig' %}

{% block body -%}
    <h1>Image edit</h1>
<img 
src= "{{ asset(entity.webPath) }}"
/>
alt= "{{ entity.alt}}"
    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('image') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
